# Console 向导完成后用 Sidebar 分页，Release 进 Preferences

设置窗从整页滚动改为向导门闩 + Sidebar，是为了最小集仍能找得到，而不是加新的产品面。曾考虑继续单栏滚动（不用改壳）和把 Release 留在全局横幅；我们改口「只导航原最小集」，增加 **Preferences** 一页，v0 只放 Release 提示（下载 / 忽略），主题与界面语言另开工单、仍放在同一页。Sidebar 指导航壳，不是以后所有功能的抽屉。打开设置窗默认进 Bridge；Preferences 在侧栏最底。
